# SRRU Check — Mobile (React Native / Expo)

Placeholder — built in **Phase 6**, after the web app and its API routes are stable.

Student-only feature set, calling the same Next.js API routes under `app/api/` as the web client.
No business logic is duplicated here; see the root [README.md](../README.md) for the full phase plan.
